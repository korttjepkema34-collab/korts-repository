# Lessons (auto-appended)

One line per code job that failed, then passed after a retry: the failure class and what fixed it.
The coder reads the last 30 lines of this file on every run. Humans may prune it.

