# Godot project

Minimal skeleton: main scene, `scripts/dev/screenshot.gd`, a gdUnit4 smoke test. Task 003 adds
the full layout (player, enemies, levels, ui, clock autoload, ENet server). Layout and rules:
`docs/09-godot-conventions.md`.

Approved assets are imported from `../assets/approved/`. Never from `incoming/`.
